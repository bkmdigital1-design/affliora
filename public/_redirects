# Cloudflare Pages redirects
# Old hash URLs redirect to new slug URLs
/#product-*  /products/:splat  301
/#article-*  /articles/:splat  301

# All other routes go to index.html (SPA fallback)
/*  /index.html  200